# 1. 从 upstream 拉取最新代码
git fetch upstream

# 2. 强制将本地 main 重置为 upstream/dev_cad
git checkout main
git reset --hard upstream/dev_cad

# 3. 强制推送到你的 fork（⚠️ 这会覆盖远程）
git push origin main --force



git merge upstream/dev_cad